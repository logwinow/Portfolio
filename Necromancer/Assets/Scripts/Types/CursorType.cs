using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum CursorType
{
    Unvisible = -1,
    Visible = 0,
    Default = 1,
    See = 2,
    Take = 3, 
    TakeItAlive = 4
}
